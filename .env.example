# Optional cloud sync (Supabase). Leave blank to use local browser storage.
# When both are set, the app stores data in Supabase so it syncs across devices.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
